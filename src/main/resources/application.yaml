spring:
  application:
    name: apt

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cyci?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: khj01040322@

  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: kr.soft.apt.dto
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

jwt:
  secret: my-super-secret-key-12345678901234567890

app:
  upload-dir: uploads/attachFile
