<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.soft.apt.mapper.SignUp.JobSeekerUserMapper">

   <select id="login" parameterType="String" resultType="JobseekerLoginDTO">
       select
           JOBSEEKER_IDX, JOBSEEKER_EMAIL,JOBSEEKER_PASSWORD
       from
            jobseeker_users
       where
           JOBSEEKER_EMAIL=#{value}
        LIMIT 1
   </select>

    <insert id="signup" parameterType="JobSeekerUserDTO">
        INSERT INTO JOBSEEKER_USERS (
        JOBSEEKER_EMAIL,
        JOBSEEKER_PASSWORD,
        JOBSEEKER_NAME,
        CREATE_AT,
        JOBSEEKER_PHONE,
        JOBSEEKER_BIRTH

        )
        VALUES (
        #{jobSeekerEmail},
        #{jobSeekerPassword},
        #{jobSeekerName},
        sysdate(),
        #{jobSeekerPhone},
        #{jobSeekerBirthDate}

        )
    </insert>


</mapper>